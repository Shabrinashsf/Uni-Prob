#include <stdio.h>
#include <string.h>
#define x 97

int main()
{
    char s[1000];
    gets(s);
    int len = strlen(s);
    char modus[26]; // a itu indeksnya 0

    for (int i = 0; i < len; i++){
        if (s[len] == x + i);

    }

}